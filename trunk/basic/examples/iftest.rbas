'multiline if test
'requires basic client to compile

IF 1=1 THEN
  PRINT "t"
  PRINT "next"
ELSE
  PRINT "failed"
ENDIF

IF 1=2 THEN
  PRINT "failed"
  END
ELSE
  PRINT "t"
ENDIF

IF 2=2 THEN
  PRINT "good"
ENDIF

IF 2=1 THEN
  PRINT "failed"
  END
ENDIF

PRINT "done"
