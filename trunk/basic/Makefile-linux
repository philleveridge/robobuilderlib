###############################################################################
# Makefile for the project RBASIC
###############################################################################

## General Flags
PROJECT = Basic
MCU = atmega128
TARGET = Basic

## Attempt to tell what platform we're on, to pick the right avr-gcc

CC = gcc


## Options common to compile, link and assembly rules
COMMON = -mmcu=$(MCU)

## Compile options common for all C compilation units.
#CFLAGS = $(COMMON) -D AVR
CFLAGS = -D LINUX -D PARSE -D IMAGE
CFLAGS += -g -Wall  -std=gnu99 # -gdwarf-2 -funsigned-char -funsigned-bitfields -fpack-struct -fshort-enums
CFLAGS += -MD -MP -MT $(*F).o -MF dep/$(@F).d 

## Assembly specific flags
ASMFLAGS = $(COMMON)
ASMFLAGS += $(CFLAGS)
ASMFLAGS += -x assembler-with-cpp -Wa,-gdwarf2

## Linker flags
#LDFLAGS = $(COMMON)
LDFLAGS +=  -Wl,-Map=RBASIC.map 


## Intel Hex file production flags
HEX_FLASH_FLAGS = -R .eeprom

HEX_EEPROM_FLAGS = -j .eeprom
HEX_EEPROM_FLAGS += --set-section-flags=.eeprom="alloc,load"
HEX_EEPROM_FLAGS += --change-section-lma .eeprom=0 --no-change-warnings


## Objects that must be built in order to link

OBJECTS = basic.o edit.o fix_fft.o linux.o linux-remote.o express.o  functions.o  oexpress.o  lists.o commands.o jpeg.o  #sock-linux.o
LIBS = -lpthread -lm -ljpeg


## Objects explicitly added by the user
LINKONLYOBJECTS = 

## Build
all: $(TARGET)

## Compile
%.o: %.c
#	$(CC) $(INCLUDES) $(CFLAGS) -DPSD_SENSOR -c  $<
	$(CC) $(INCLUDES) $(CFLAGS) -c  $<
	
	
##Link
$(TARGET): $(OBJECTS)
	 $(CC) $(LDFLAGS) $(OBJECTS) $(LINKONLYOBJECTS) $(LIBDIRS) $(LIBS) -o $(TARGET)


## Clean target
clean:
	-rm -rf $(OBJECTS) 


## Other dependencies
-include $(shell mkdir dep 2>/dev/null) $(wildcard dep/*)

